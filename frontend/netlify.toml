[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev"
  port = 5173

# SPA fallback for Vite/React routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
